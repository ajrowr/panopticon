## Add instructions about unpacking Raspbian image

```
sudo apt-get update
sudo apt-get install hostapd dnsmasq
```


## Apply the files in this repo
## Use raspi-config to expand partition and boot to console
